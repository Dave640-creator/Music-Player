name: smart_music_player
description: Mosaic Player — a smart offline media experience. Discover, Library, Now Playing and Control Center organized around how people actually listen.
publish_to: 'none'
version: 2.0.0+2

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Database
  sqflite: ^2.3.3
  sqflite_common_ffi: ^2.3.4
  path: ^1.9.0

  # File & Storage
  path_provider: ^2.1.3
  file_picker: ^8.0.7
  permission_handler: ^11.3.1

  # Audio Playback
  just_audio: ^0.9.40
  just_audio_background: ^0.0.1-beta.13
  audio_session: ^0.1.21

  # Video Playback
  video_player: ^2.10.0

  # Sharing
  share_plus: ^10.1.3

  # State Management
  provider: ^6.1.2

  # Charts & UI
  fl_chart: ^0.68.0
  google_fonts: ^6.2.1
  shimmer: ^3.0.0

  # Utilities
  intl: ^0.19.0
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
